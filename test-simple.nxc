// Complete error detection test

// Test 1: Valid code
task main() {
  int x = 10;
  OnFwd(OUT_A, x);
}

// Test 2: Unclosed brace
task errorTest() {
  int y = 5;
  OnFwd(OUT_A, y);
  // missing closing brace

// Test 3: Unclosed parentheses
task parenError() {
  OnFwd(OUT_A, 75;
}

// Test 4: Unterminated string
task stringError() {
  string msg = "Hello World;
  PlaySound(msg);
}

// Test 5: Unused variable (for semantic analysis)
task unusedVar() {
  int unused = 42;
  int used = 10;
  OnFwd(OUT_A, used);
}

// Test 6: Undefined function (for semantic analysis)  
task undefinedFunc() {
  UndefinedFunction();
}

// Test 7: Very long line to test warning
task longLine() {
  int veryLongVariableNameThatExceedsTheRecommendedLineLengthAndShouldGenerateAWarningAboutLineLengthExceedingTheMaximumAllowedLength = 1;
}

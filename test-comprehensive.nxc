// Comprehensive NXC Test File - Edge Cases and False Positives

#define STRING_WITH_QUOTES "a string with \"quotes\""
#define MULTILINE_MACRO(a, b) \
  a + b;

// --- String Edge Cases ---
string s1 = "hello world";
string s2 = "a string with an escaped quote: \"";
string s3 = "a string with a backslash: \\"; // keep simple and valid
string s4 = ""; // Empty string

// This is a valid use of the preprocessor to define a function-like macro
#define IS_ODD(n) ((n) % 2 != 0)

// Global declarations
int a = 1;
int b = 2;
int c = 3;
int g = 2;

// --- Bracket Balancing and runtime checks inside main ---
task main() {
  // Nested parentheses
  int x = (1 + (2 * 3));

  // Example loop — must be inside a function/task
  int i;
  for (i = 0; i < 10; i++) {
    // no-op
  }

  // Operator edge cases executed inside main
  int result = a + b * c;
  result = a + b; // valid assignment inside function

  // Comment edge cases
  int d = 1; /* This is a comment */ int e = 2;

  // Semicolon tests (kept valid)
  int f = 1;
  
}